name: JSON check

on:
  push:
    paths:
      - '**.json'
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: '2'
      - name: deletion-check
        uses: raphaelahrens/gsd-tools/actions/deletion-check@actions
      - name: json-check
        uses: raphaelahrens/gsd-tools/actions/json-check@actions
